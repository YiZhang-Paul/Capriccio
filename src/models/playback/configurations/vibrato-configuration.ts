export class VibratoConfiguration {
    public frequency: number;
    public depth: number;

    constructor(frequency: number, depth: number) {
        this.frequency = frequency;
        this.depth = depth;
    }
}
